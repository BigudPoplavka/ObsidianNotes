 #Функция #Алгоритмы #Итерационные_методы #Оптимизация_управления 
[[Экстремум функции]]
[[Градиент]]

- ## Введение

Для оптимизации, оптимального управления состоянием объектов необходимо иметь их математические модели, поскольку оптимальные состояния сначала ищут на моделях, а потом переносят на объекты. 

Дифференциальные уравнения (с независимыми пространственными переменными ` x, y, z`, иногда `t`), которыми описываются распределенные системы, их **коэффициенты**, **свободные члены** или **краевые условия** могут содержать **переменный параметр** или **вектор переменных параметров**, изменяя значение которого, мы можем регулировать состояние (решение) системы дифференциальных уравнений. 

- # Такой параметр, обычно, называют управлением

В большинстве своём исторически сложившиеся подходы к решению задач оптимизации, т.е. к нахождению параметра 𝑢, являются непрямыми, поскольку они сначала требуют получения необходимых условий оптимальности в той или иной форме, а только потом, из полученных условий, ищут параметр 𝑢.
## Прямой экстремальный подход

Применяется в решении задач **оптимизации управления**, когда оптимальное решение `u*` является **аргументом задачи минимизации целевой ф-ции от управления** `u`:

![[Pasted image 20231010152748.png]]

**Экстремум** - минимальное или максимальное значение **функции**. В **прямом экстремальном подходе** решение задачи оптимизации как раз и сводятся к нахождению **оптимального значения целевого критерия** `U*` - при котором функция достигает экстремума.

Существуют методы для одномерных и **n**-мерных функций - то есть зависящих от разного числа параметров. Искать экстремум имеет смысл там, где он единственный и имеет физический смысл. Соответственно выделяется **отрезок допустимого управления** - `[a; b]` 

Самые простые из методов:
	1. [[Метод дихотомии]]
	2. [[Метод золотого сечения]]

В своей основе эти методы также **итерационные**

![[Pasted image 20231010152931.png]]

Данное вычисление делается **для каждой компоненты вектора управления u**.
Далее производится смещение вдоль направления `p(uk)`, которое может определяться через производные, говорящие о скорости изменения функции, и шаг `bk` - число, определяющее шаг смещения.

Если направление `p(uk)` выражено **градиентом**, то мы прибегнем к градиентным методам, движущимся против градиента:
	1. [[Метод наискорейшего градиентного спуска]]
