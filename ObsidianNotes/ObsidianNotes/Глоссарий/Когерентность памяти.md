[[Многопроцессорность]]

**Когерентность памяти** - свойство **многопроцессорных или многоядерных** компьютерных систем с доступом к **одной** области памяти, заключающееся в том, что изменённая одним ядром/процессором ячейка памяти принимает новое значение для остальных ядер/процессоров.